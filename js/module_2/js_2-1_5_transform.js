// Написать скрипт который обьеденяет скрипт в одно строковое значение.
// - Элементов может быть произвольное кооличество
// - Пусть элементы массива в строке буду  разделены запятой
// - Сначала через for
// - Потом через join()

const friends = ['Mango', 'Poly', 'Kiwi', 'Ajax'];

                      //    Решение
    
                      // - Сначала через for
// let string = '';
// for (const friend of friends) {
//     string += friend + ',';
// }
// string = string.slice(0, string.length - 1);
                   

                  //  - Через join

// const string = friends.join(', ');


console.log(string);
